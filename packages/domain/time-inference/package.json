{
  "name": "@workspace/time-inference",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsup src/index.ts --format esm --dts --out-dir dist --tsconfig tsconfig.build.json",
    "clean": "rm -rf dist",
    "test": "tsx --test src/engine/scoring.test.ts src/survey/weight-engine.test.ts"
  },
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@workspace/engine-data": "workspace:*",
    "@workspace/saju-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:tooling",
    "tsup": "catalog:tooling",
    "tsx": "catalog:tooling",
    "typescript": "catalog:tooling"
  }
}
