{
  "name": "@workspace/engine-data",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsup src/index.ts --format esm --dts --out-dir dist --tsconfig tsconfig.build.json && mkdir -p dist/data && cp src/data/engine.json dist/data/",
    "clean": "rm -rf dist"
  },
  "exports": {
    ".": "./dist/index.js",
    "./data/engine.json": "./dist/data/engine.json"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "tsup": "catalog:tooling",
    "typescript": "catalog:tooling"
  }
}
