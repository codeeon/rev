{
  "name": "@workspace/saju-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsup src/index.ts --format esm --dts --out-dir dist --tsconfig tsconfig.build.json",
    "clean": "rm -rf dist",
    "test": "tsx src/validation.spec.ts",
    "typecheck": "tsc --project tsconfig.json"
  },
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "tsup": "catalog:tooling",
    "tsx": "catalog:tooling",
    "typescript": "catalog:tooling"
  },
  "dependencies": {
    "lunar-javascript": "catalog:"
  }
}
