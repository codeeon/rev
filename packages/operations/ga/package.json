{
  "name": "@workspace/ga",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsup src/index.ts --format esm --dts --out-dir dist --tsconfig tsconfig.build.json",
    "clean": "rm -rf dist",
    "typecheck": "tsc --project tsconfig.json"
  },
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@workspace/observability-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:tooling",
    "tsup": "catalog:tooling",
    "typescript": "catalog:tooling"
  }
}
